# AdMind 项目工程交接文档

> 最后更新：2026-08-21<br>
> 当前公开版本：`v0.3.0`（阶段 1A 固定帧回归基线）<br>
> 公开 GitHub：`https://github.com/Owl-Lee/AdMind`<br>
> 公开演示：`https://admind-decision-console.liyanbao06.chatgpt.site/`（无需登录）<br>
> 代码事实来源：公开仓库 `main`、本交接文档和 `CHANGELOG.md`。私有归档不是“正式版”，也不是日常开发源；在归档处置方案确定前不要删除。<br>
> 给下一位工程师的话：这个项目已经越过了“只有想法和静态页面”的阶段。接手后先稳定、验证和讲清楚现有链路，不要一上来继续堆功能。

## 交接使用原则 / Working agreement

> **文档保存事实，聊天处理决策。文档是长期记忆，对话是当前阶段的工作台。**

- 一个对话只处理一个明确小阶段；结束时把“完成、验证、遗留、下一步”写回交接文档。
- 新对话只需要提供工程路径、交接文档路径和本阶段目标，不需要复制全部聊天记录。
- 大型设计讨论、实际编码和 GitHub/发布操作尽量拆开，避免当前决策被历史上下文淹没。
- 如果旧记录与本页顶部的“当前状态”或最后一节冲突，以较新的事实记录为准；历史章节保留用于追溯。

English summary: documents preserve verified project facts; chat is the short-lived workspace for the current decision. Keep one small phase per task, write completion/verification/remaining work/next step back here, and treat the public `main` branch as the code source of truth.

## 一、项目定位

AdMind 是一个面向长视频平台的 AI 广告决策原型。它不是广告拦截器，也不是简单把大广告缩小，而是依次回答三个问题：

1. **S1 高潮避让：什么时候适合投放？**
2. **S2 暂停保护：暂停后要不要投、用什么形式、放在哪里？**
3. **S3 伦理保护：这个场景能不能投？**

核心原则：

> AI 负责提供“视频里发生了什么”的证据；AdMind 的确定性规则负责最终决定。商业压力不能越过伦理硬规则。

## 二、项目位置与入口

- 本地工程：克隆公开仓库后，以仓库根目录为工程目录
- 线上演示：<https://admind-decision-console.liyanbao06.chatgpt.site/#demo>
- 决策方式：<https://admind-decision-console.liyanbao06.chatgpt.site/#decision>
- S2 回归实验室：<https://admind-decision-console.liyanbao06.chatgpt.site/regression>
- 公开仓库：<https://github.com/Owl-Lee/AdMind>
- Sites 项目 ID：`appgprj_6a7c0083237c819185c04e9fd932fba7`
- 公开主分支：`main`；功能修改先进入短生命周期分支，通过 CI 后合并。
- 配套学习文档：[`docs/AdMind项目技术亮点与讲解.md`](AdMind项目技术亮点与讲解.md)

常用命令：

```powershell
pnpm dev
pnpm typecheck
pnpm test:unit
pnpm build
pnpm check
```

如果默认 Node 或 pnpm 不可用，请先按照 [`DEVELOPMENT.md`](DEVELOPMENT.md) 安装项目要求的版本，再执行验证命令。

## 三、当前真实完成情况

| 模块 | 已完成的真实能力 | 当前边界 |
|---|---|---|
| S1 高潮避让 | CHARGE、Coffee Run、Llamigos 已有缓存的真实 TwelveLabs 分析；播放器可比较固定插播与 AdMind 延后结果；右侧动态展示当前片段、张力、证据分数与决定 | 模型结果是片段级，不是逐帧张力曲线 |
| S2 暂停保护 | 已监听暂停、恢复、拖动、页面可见性和焦点；稳定暂停后用本地 MediaPipe 检测当前帧；计算避让区域；先显示静音小卡片，长暂停可升级全屏；恢复播放时清理广告和检测框 | 动画人物、动物和复杂背景仍有漏检/误检；目前选择高召回、保守避让 |
| S3 伦理保护 | 公开站点提供海上救援、医疗转运两种可选素材及真实 TwelveLabs 结果；伦理硬规则可直接阻止片中广告；右侧展示伦理上下文、模型证据和最终阻断 | FEMA 灾后分析 JSON 仅作为历史研究记录保留，对应视频未进入公开资产清单，也不是当前可选场景；现有两段素材不能代表所有敏感语境 |
| 决策方式 | 已用较简洁的流程说明 TwelveLabs、标准化数据、规则引擎和播放器执行的关系 | 深层审计数据主要仍在代码和 `docs/` 中 |
| 视觉 | 主页面采用浅色、圆角卡片、液态胶囊和紫色系统；演示与决策方式位于同一站点；英文默认、中文完整可切换；字幕与文档语言同步；360–1440 CSS 像素宽度已回归 | 仍需在未来新增组件时同时维护两种语言与窄窗断点 |

历史记录：第 31 版当时完成的修正：

- 中文页面的普通界面文案已统一为中文，仅保留 AdMind、TwelveLabs、API 等专名；
- “名义机会点”已改为“原定广告候选点”；
- S1/S3 右侧面板显示当前分析片段及起止时间；
- 张力改为真实片段评分，例如 `9.0 / 10`；
- 模型证据显示原始分数，例如 `0.95`，并明确它不是概率论中的置信区间；
- S3 切换素材时强制重新挂载播放器，增加可播放和错误状态，修复长期停在加载状态的问题；
- 类型检查、26 项单元测试和正式构建均已通过。

## 四、关键技术链路

```text
视频素材
  ↓
TwelveLabs 离线分析（S1 / S3）或 MediaPipe 当前帧分析（S2）
  ↓
标准化 JSON：时间段、标签、分数、风险与位置
  ↓
AdMind 确定性规则：播放器状态、伦理边界、素材形式、商业约束
  ↓
可执行广告计划：立即投、延后、小卡片、全屏或完全不投
  ↓
网页播放器执行，并把证据和决定显示给用户
```

必须坚持的表达边界：

- `0.95` 是模型证据分数，不是统计学 95% 置信区间，也不是保证校准过的正确概率；
- 张力 `9.0 / 10` 是一个分析片段共同使用的粗粒度结果，不是每一秒都独立计算出 90%；
- S2 只能观察当前网页和播放器事件，不知道用户脑内意图，也看不到用户切到了微信还是 Word；
- 网页演示读取缓存的 TwelveLabs JSON，因此访客不需要 API Key，也不会每次播放都花 API 费用；
- S3 的商业任务不能为了补量越过伦理规则。

## 五、代码地图

| 路径 | 作用 |
|---|---|
| `app/components/ShowcaseDemo.tsx` | 主演示、播放器、三类动态证据面板、S2 状态机和广告展示 |
| `app/lib/face-detector.ts` | MediaPipe 人脸/主体检测、去重和检测参数 |
| `app/lib/pause-decision.ts` | S2 安全区域和暂停决策逻辑 |
| `app/lib/pause-regression.ts` | 固定帧标注校验、目标匹配和基线指标重算 |
| `app/regression/` | 双语 S2 浏览器回归实验室 |
| `evaluation/s2/` | 规则初标、待产品负责人复核项、调参前原始预测和回归说明 |
| `public/evaluation/s2/frames/` | 带 SHA-256 的不可变固定帧 |
| `app/page.tsx` | 把三类素材、分析 JSON 和决策结果注入页面 |
| `packages/video-analyzer/` | TwelveLabs/Gemini 供应商适配、Prompt、标准化与 CLI |
| `packages/decision-engine/` | 硬规则、候选方案、排序与测试 |
| `analysis/runs/` | 页面实际读取的标准化 TwelveLabs 结果 |
| `analysis/raw/` | 供应商原始输出，用于追溯，不直接展示 |
| `docs/` | PRD、架构、决策规范、研究矩阵和验收标准 |
| `.openai/hosting.json` | Sites 项目标识；存在该文件时发布必须使用 Sites 工作流 |

## 六、当前已知问题与风险

### P0：下一轮必须先确认

1. **S3 视频切换虽然已修复和发布，但还没有得到用户完整回归确认。** 需要依次测试当前公开的海上救援、医疗转运：能加载、能拖动、能播放、切换后标题和证据同步变化。FEMA 灾后素材已经退出公开选择器，不再列入当前验收。
2. **S1/S3 当前片段面板需要实际播放验证。** 时间跨过分析片段边界时，标签、时间范围和分数应该改变；不得继续给人“写死 90%”的感觉。
3. **S2 检测仍是工程权衡。** 阶段 1A 在 13 张规则明确的初标固定帧上量化出 36.4% 保护目标召回率、16.0% 精确率和 30.8% 危险位置误投率；这些标签由代理依据既定规则起草，尚不能称为人工或产品负责人已审核。当前检测不能宣传成高精度角色理解模型，阶段 1B 必须先压低危险误投，再处理误检和过度顺延。

### P1：产品完成度问题

4. **中英文切换已经实现。** 公开页面默认英文，中文为完整模式；统一本地化层同步可见文案、动态属性、HTML 文档语言和字幕轨道。后续新增文案仍必须进入覆盖测试。
5. **浏览器回归已经完成一轮，但尚未固化为仓库内 E2E。** 当前已使用真实浏览器测试导航、素材/策略、音量、广告关闭、语言往返和多尺寸布局；下一步应把核心路径转成可重复的浏览器测试文件。
6. **README 和核心交接资料已同步。** README 保持英文完整内容在上、中文完整内容在下，并与当前三场景、双语站点和公开边界一致；较早的研究/spec 文档仍作为历史设计依据。
7. **真实效果指标尚未验证。** “减少打扰、提高完成率、兼顾商业价值”仍是产品假设，不能写成已经通过 A/B 实验证明。

### P0：发布 GitHub 前必须处理

8. 早期 `Sprite Fright` 可选样本没有形成可复现的公开素材流程，已从公开选择器与发布包中移除；当前公开克隆不会出现指向缺失视频的入口。
9. 页面继续使用 `public/game-ad-clean.png`。项目所有者已于 2026-08-19 明确确认该图属于自己的项目素材并可用于公开作品集；未使用的原始截图副本不进入公开发布快照。
10. API Key 只能放在本地 `.env.local`，绝不能写入代码、文档、聊天、Git 或前端 bundle。公开前必须做 secrets 扫描。

## 七、后续任务拆分

原则：一次对话只完成一个小阶段。完成后先提醒用户、报告验证结果，再决定是否进入下一阶段。

### 阶段 0：接管和线上回归（20–30 分钟）

- 完整读取本交接文档和《AdMind项目技术亮点与讲解.md》；
- 检查 `git status`，保留用户所有未提交素材；
- 静音测试线上第 31 版；
- S1 三段素材各测试一次策略切换、拖动、投放点和证据栏；
- S2 测试暂停、短暂停顺延、拖动取消、页面切换、卡片位置和长暂停全屏；
- S3 测试三段素材加载、切换、拖动和伦理阻断。

完成标准：输出一张“通过 / 失败 / 未验证”清单，只修阻断性 Bug，不顺手重构。

### 阶段 1：稳定 S2 检测（进行中）

**1A，已完成：建立回归样本与基线。** 已从公开仓库内授权明确的《CHARGE》素材固定截取 20 张 1280×720 关键帧，保存校验和、保护目标、可接受广告位置、13 个由代理按既定规则起草的明确初标与 7 个待产品负责人复核的诊断样本。旧计划中的 Sprite Fright 没有进入公开数据集。

**1B，下一步：校准检测。** 根据 13 个规则明确初标样本调整人脸/主体阈值、框去重、最小尺寸、真实广告占位和区域风险；7 个争议样本在产品负责人复核前不进入阻断门槛。比较调整前后，不凭一张截图来回调参。

**1C：固化浏览器基准。** 把 MediaPipe WASM 固定到本地，增加独立浏览器基准任务，并为状态机、旧结果清理和高召回兜底补齐测试。

完成标准：在固定回归帧上给出漏检、误检和安全位置命中结果；检测框恢复播放后不残留；不能只说“感觉有 70%”。

### 阶段 2：把 S1/S3 证据栏做成可信产品（建议拆成 2 次对话）

**2A，30–45 分钟：语义与动态验证。** 检查三个 S1、三个 S3 素材的片段边界、中文标签、分数来源和最终决策是否一致。  
**2B，30–45 分钟：异常与低置信度。** 增加无当前片段、低证据分数、视频加载失败和分析缺失时的保守文案与兜底。

完成标准：页面清楚区分“模型观察”和“规则决定”；不存在英文残留、假实时或把模型分数说成统计概率的问题。

### 阶段 3：真正的中英文网站 — 已完成

**3A，45–60 分钟：国际化基础。** 建立统一文案字典和语言状态，中文为默认；不得为了翻译复制整套组件。  
**3B，45–60 分钟：全站翻译与回归。** 覆盖演示、决策方式、状态、错误和辅助文本，检查两种语言布局。

完成标准：中文模式没有普通英文界面词，英文模式没有普通中文界面词；专有名词保持原名；刷新后语言选择可稳定保留。

### 阶段 4：GitHub 级交付 — 已完成公开版，持续维护

**4A，45–60 分钟：仓库清理。** 处理大视频、素材授权、`.gitignore`、License 和 secrets 扫描。  
**4B，45–60 分钟：README 与架构。** 更新中英文 README、截图、演示地址、真实/模拟边界、安装与分析命令。  
**4C，45–60 分钟：发布候选。** 全量 `pnpm check`、从干净克隆启动、线上回归、打标签并写 Release Notes。

完成标准：陌生人从干净克隆可以按照 README 跑起来；没有密钥和侵权素材；线上 Demo 与仓库说明一致。

### 阶段 5：再考虑增强功能

- 上传新视频后自动进入 TwelveLabs 分析队列；
- 分析结果人工复核与修正；
- 多供应商比较与失败回退；
- 真实 A/B 实验设计和指标采集；
- 持久化决策历史与批量视频处理。

这些都不是下一轮任务。现有三场景稳定、可解释、可复现之后再做。

## 八、下一位工程师的工作纪律

1. 开工前先说本轮唯一目标，禁止同时重做 UI、模型、素材和 GitHub 发布。
2. 工具执行期间每 60 秒内给用户一次简短进度；视频和测试必须静音，不能在用户电脑上突然播放声音。
3. 先读代码和真实 JSON，再判断数据是否写死；不得为了看起来动态而伪造随机小数。
4. 同一错误连续两次或 15 分钟没有新证据，停止空转，说明根因和下一步。
5. 不覆盖用户已有修改，不使用 `git reset --hard`、`git checkout --` 等破坏性操作。
6. 每个任务完成时必须主动提醒用户，说明：完成了什么、怎么验证、还剩什么、建议下一步是什么。
7. 每完成一个阶段，把结果和新发现补回本交接文档或技术亮点文档；不要让关键能力再次变成“隐藏功能”。
8. 未经用户确认，不把“预置 Demo 可用”宣传成“生产系统已经解决”。

统一的完成提醒格式：

```text
【AdMind 阶段 X 已完成】
完成：……
验证：……
未解决：……
下一步建议：……
本轮没有擅自扩大任务范围。
```

## 九、下一次对话直接使用的开工指令

```text
请先完整读取桌面的《AdMind项目工程交接文档.md》和《AdMind项目技术亮点与讲解.md》。
本轮只做“阶段 0：接管和线上回归”，不要增加新功能、不要重构 UI、不要重新调用付费视频 API。
先确认工程路径、git status、当前提交和线上第 31 版；测试必须全程静音。
按 S1、S2、S3 输出“通过 / 失败 / 未验证”清单，只修复阻断性 Bug。
工具执行期间每 60 秒内给一次简短进度；同一错误两次或 15 分钟无新证据就停止并报告。
完成后用交接文档规定的格式提醒我，并把结果补回交接文档。
```

## 十、交接结论

AdMind 现在已经具备一个有技术含量的作品集项目骨架：真实视频理解 API、缓存和标准化 JSON、确定性决策、播放器状态机、本地视觉检测、伦理硬规则，以及能让用户直接看到决策过程的动态界面。

当前最重要的工作不是继续证明“还能加多少功能”，而是证明这套链路稳定、真实、可解释，并能从一个干净仓库被别人复现。下一位工程师应从阶段 0 开始，完成一项、提醒一次、记录一次。

—— 本轮工程师下班，钥匙、地图和坑位都留在这里了。

## 十一、阶段 0 记录（2026-08-15，静音自动检查）

以下内容保留当日历史状态；其中 FEMA 灾后场景后来因公开资产来源未达到当前发布标准而退役，当前公开站点只保留海上救援和医疗转运。请以本文第三、六、二十节作为现状依据。

本次只进行接管检查和不播放声音的线上回归，**阶段 0 尚未完整完成**。

### 已确认

- 工程路径可访问；当前分支为 `sprite-publish`，当前提交为 `15b548c Clarify analysis scores and localize signals`；
- 当时唯一未跟踪文件为 `public/sprite-fright-pause-demo-540p.mp4`；该历史状态已在 2026-08-19 的公开发布准备中处理，不再作为公开页面选项；
- Sites 项目 ID 为 `appgprj_6a7c0083237c819185c04e9fd932fba7`，线上第 31 版可打开；
- S1 的动作冲突、情绪连续、追逐高潮三段素材均可切换和加载，且视频源、时长与文案随素材变化；AdMind 面板显示当前分析片段、起止时间、片段级评分和“模型证据评分，非统计学置信区间”；
- S2 的简单角色画面和复杂角色画面均可切换并完成加载；复杂素材在等待加载后恢复可用进度条；
- S3 的海上救援、医疗转运、灾后现场三种选择均可切换；灾后现场在 AdMind 策略下显示“硬规则拦截：本片不投放”、伦理上下文和对应分析片段。

### 未验证（保留给后续全程静音人工回归）

- S1/S3 在实际播放和跨越分析片段边界时，证据栏是否随时间更新；
- S1 的策略切换、拖动和实际投放点；
- S2 的暂停、短暂停顺延、拖动取消、页面隐藏/失焦、卡片位置、长暂停全屏，以及恢复播放后的检测框清理；
- S3 三段素材的实际播放与拖动，以及切换后的标题和证据在播放过程中的同步变化。

### 备注

线上自动检查环境只能只读操作媒体状态，不能预先静音视频或安全设置播放进度；为遵守“测试全程静音”，本次没有触发播放。未验证项目不能据此视为通过，也没有发现足以授权修复的阻断性 Bug。

### 补充回归结果（2026-08-15）

本次使用线上时间轴的静音跳转模拟拖动，不播放媒体：

| 场景 | 通过 | 未验证 |
|---|---|---|
| S1 高潮避让 | 三段素材均可切换、加载、在传统/AdMind 策略间切换；时间轴跳转后证据栏随片段边界变化；未发现控制台错误。动作冲突从“核心能量被盗（00:00–00:12）”切至“激烈对抗（00:12–01:29）”；情绪连续从“时间循环蒙太奇（00:00–00:19）”切至“求婚被拒（00:19–00:31）”；追逐高潮从“开场铺垫（00:00–00:12）”切至“危机解除与揭示（00:51–01:15）”。 | 实际连续播放时的自然跨段更新，以及真实广告展示时机。 |
| S2 暂停保护 | 两段素材均可切换、加载和静音时间轴跳转；AdMind 策略页面能显示播放器信号、页面状态和等待有效暂停信号；跳转后状态机记录拖动且无残留广告/检测框。 | 从播放中触发暂停、短暂停顺延、长暂停视觉分析、页面隐藏/失焦、恢复播放清理与卡片位置；这些都必须在可确认静音的人工回归中完成。 |
| S3 伦理保护 | 海上救援、医疗转运、灾后现场均可切换、加载和静音时间轴跳转；三段在 00:15 均展示伦理上下文和模型证据，并显示“硬规则拦截：本片不投放”。 | 实际连续播放与拖动过程中的标题/证据同步确认。 |

结论：本轮未发现阻断性 Bug；阶段 0 的自动静音部分已完成。剩余项目属于需要播放媒体事件的人工静音回归，未经确认不得标为完全通过。

## 十二、展示与播放器同步记录（2026-08-16，Sites 第 45 版）

- 展示页保持“首页 → S1 → S2 → S3”的四幕结构，但已经取消滚轮劫持与 CSS 滚动吸附，恢复普通连续滚动；顶部场景按钮仍可主动平滑跳转。
- 所有场景播放器新增小喇叭音量入口，点击后展开较长的竖向音量条；默认音量为 65%，避免高响度素材突然冲击音响。
- S2 拖动次数改为按一次完整的按下—拖动—松开计数，不再按浏览器连续触发的 `seeking` 事件累加。
- S2 的位置风险文案由“采用 / 对照”改为“推荐位置 / 风险最高”；两项分别表示四个候选角落中遮挡风险最低和最高的区域。
- S2 待交付队列说明已经移到播放器与证据面板下方并提高字号。
- 首页抽象几何动画替换为静音循环的真实 S1 素材预览；底部滚动提示文字已移除，保留无文案的气泡装饰。
- 本轮通过类型检查、26 项单元测试和正式构建；当时 `public/sprite-fright-pause-demo-540p.mp4` 仍为未跟踪用户素材。该历史状态已在 2026-08-19 的公开发布准备中处理。

第 46 版继续修正播放器状态：音量入口改为标准高音量、低音量、静音三态图标，浮层内可一键静音或恢复；点击控件外任意位置或按 `Esc` 会关闭音量浮层。全屏广告左上角新增“跳过广告”。S2 广告一旦实际展示，就不会因截图工具导致的页面失焦或隐藏而回滚成待交付；恢复播放记为完成，主动关闭记为已展示后跳过。只有广告尚未出现、仍在确认稳定暂停或分析画面时被打断，才进入待交付队列。

第 47 版收简音量交互：删除浮层内重复的静音喇叭，只保留播放器控制栏里的唯一音量入口；竖向音量条缩短、变细，并用紫色已选区与半透明剩余区表达当前音量。拖到最低即显示静音图标，点击外部或按 `Esc` 仍会自动收起浮层。

S1 的商业兜底边界：当整片持续高张力时，不等于永远不投广告。系统先寻找相对低谷并降级为更低遮挡形式；如果相对低谷仍超过允许的打断上限，则把任务转给片前、片后、暂停场景、伴随式广告或后续内容，而不是强插中贴片。S3 伦理硬规则始终不能被补量要求覆盖。当前 Demo 已实现窗口内不强投和交付缺口记录，跨页面持久化任务队列仍未实现。

## 十三、v0.2.1 阶段性收尾记录（2026-08-20）

### 完成

- 公开网站默认英文，并提供完整中文模式；连续中英往返不会累积翻译或出现标题拼接缺空格。
- 语言切换会同步 `<html lang>`、动态 `aria-label`/`alt`、可见文案和 `charge-demo-en.vtt` / `charge-demo-zh.vtt` 字幕轨道。
- 修复窄窗口中播放器标题竖排、跳转按钮被切边、策略开关展示一半、首页最小内容宽度撑破容器和场景导航露半个标签的问题。
- 播放器在 1280 像素以下提前把状态、动作和策略控件完整分行；360 像素时仍无横向页面溢出。
- 音量控件保持单一喇叭入口，可调 0–100%，动态图标和辅助名称正确；播放、外部点击或 Escape 会优先收起音量条。
- 全屏广告提供明确的“跳过广告 / Skip ad”，关闭后不会把已经产生的展示错误回滚为“尚未投放而顺延”。
- README 已保持英文完整版在上、中文完整版在下；版本、路线图、交接和变更记录已与当前公开体验对齐。

### 验证

- 自动质量门：ESLint、TypeScript、30 项单元/集成测试、5 项渲染/依赖补丁测试和生产构建全部通过。
- 浏览器尺寸：`1440×900`、`1024×768`、`768×900`、`600×900`、`430×900`、`360×800`；英文和中文均为 0 横向溢出，当前视口内没有交互控件越界。
- 浏览器交互：主导航、S1/S2/S3 场景导航、素材切换、传统/AdMind 策略切换、音量展开/调整/收起、广告展示/跳过和三轮中英往返均已测试。
- 无障碍基础：按钮和链接均有可访问名称，输入控件均有标签，图片均有 `alt`，页面无重复 `id`。
- 公开访问：线上 Demo 与 GitHub 仓库均以未登录 HTTP 请求返回 200；Demo 不出现登录表单。

### 遗留

- S2 下一阶段仍应建立 15–20 张固定暂停画面回归集，量化漏检、误检、位置命中率和拒投准确率；当前只能证明交互闭环和保守避让逻辑稳定。
- 把本轮真实浏览器验收固化为仓库内自动 E2E，尤其覆盖媒体事件、页面失焦/隐藏和长暂停全屏升级。
- 当前的“待交付任务”仍是会话内状态，尚未实现跨页面或跨设备的持久化编排。

### 下一步

进入阶段 1A：建立 S2 固定回归样本、规则初标与产品负责人复核流程。不要先继续改视觉，也不要把预置 Demo 的通过结果包装成生产准确率。

## 十四、v0.2.2 语言恢复补丁（2026-08-20）

- 服务端和首次客户端渲染统一使用英文，避免曾选择中文的回访用户出现 hydration 不一致。
- 页面挂载后再读取并恢复 `admind-locale`；英文仍是陌生访客的公开默认语言，用户主动选择仍会被保留。
- 本补丁已通过 ESLint、TypeScript、30 项单元/集成测试、5 项渲染/依赖测试和生产构建。
- 浏览器已分别验证“保存中文后刷新”和“保存英文后刷新”；文档语言、界面语言和字幕语言保持同步，控制台无 hydration 警告。

## 十五、v0.2.3 缓存媒体就绪补丁（2026-08-20）

- 部署后验收发现：媒体命中缓存且加载极快时，`loadedmetadata` / `canplay` 可能在 React 绑定处理器前已经发生，视频实际可用但界面仍显示“正在加载视频…”。
- 播放器现在会在挂载和素材切换后主动核对 `readyState` 与 `duration`，并继续监听后续媒体就绪和错误事件。
- 已用首次加载与缓存刷新分别验证：决策点按钮出现、进度条可用；静音模式下广告能在投放点触发并可通过“跳过广告”关闭。
- 本补丁再次通过完整质量门和 GitHub 必需 CI；最终公开版本以 `v0.2.3` 为准。

## 十六、v0.2.4 广告素材显示补丁（2026-08-20）

- 修复“右侧已经显示广告投放成功，但播放器内素材不可见”的状态与渲染不一致。
- 广告图改为直接加载站内公开静态资源；S2 小卡片升级全屏时不再依赖透明度动画，并在升级后移除定位虚线框。
- 本地真实浏览器已逐帧验证 S1 传统全屏、S1 AdMind 静音卡片、S2 小卡片与 8 秒全屏升级、S3 传统全屏和 S3 AdMind 伦理禁投。
- ESLint、TypeScript、33 项单元/集成测试、5 项渲染/依赖测试和生产构建全部通过；最终公开版本以 `v0.2.4` 为准。

## 十七、v0.2.5 广告原图强制显示补丁（2026-08-20）

- 广告原图同时作为广告容器的背景兜底与前景素材，并增加版本标识绕开旧缓存。
- S2 判定投放成功后不再显示虚线定位框，而是直接在选定位置展示游戏广告原图。
- 小卡片和全屏广告共用同一张项目自有原图；恢复播放、主动关闭及全屏升级逻辑保持不变。

## 十八、v0.2.6 首页广告显示补丁（2026-08-20）

- 首页首屏演示卡片不再播放剧情视频，直接展示与场景投放共用的蓝色奇幻游戏广告原图。
- 卡片继续保留安全窗口、规则决定和证据评分，使首页同时讲清“广告是什么”与“为什么此时投放”。
- 英文与中文模式均已在真实浏览器中确认广告图、投放状态和安全窗口文案完整显示。
- 新增服务端渲染回归断言，要求首页 HTML 必须包含当前版本的广告素材地址与“广告已展示，任务已完成”状态，避免后续再次退化为空白或剧情画面。

## 十九、v0.2.7 S2 已投放广告可见性补丁（2026-08-20）

- 修复 S2 宽屏下右侧已显示“已安全展示 / 静音小卡片”，但广告层可能因倒计时状态短暂不同步而消失的矛盾状态。
- S2 现在以 `pausePhase=delivered + adResult=shown` 作为广告仍应可见的事实来源；关闭、恢复播放或拖动后才会结束展示。
- 小卡片增加明确的可见状态与层级兜底；原图仍同时作为前景图片和容器背景，八秒后升级全屏的逻辑保持不变。
- 当前帧识别仍属于单帧 MediaPipe 原型：动画角色可能被误判为人脸，人物主体框也可能过大。现阶段继续采用“宁可少投，不要遮挡主体”的保守策略；不要通过随意降低阈值伪造准确率，下一步应进入阶段 1A，用固定暂停帧回归集量化后再调参。

## 二十、v0.3.0 阶段 1A 固定帧回归基线（2026-08-21）

### 完成

- 使用公开仓库内已登记为 CC BY 4.0 的《CHARGE》片段建立 20 张 1280×720 固定暂停帧；每张图片和输入视频均保存 SHA-256。
- 建立 `evaluation/s2/manifest.json`：记录保护目标、可接受广告位置、预期展示/顺延、标签来源与复核状态。允许多个安全角落，不强迫唯一答案。
- 13 个规则明确样本由代理依据现有产品规则完成初标并进入当前基线；这 13 个标签不能称为人工审核或产品负责人已审核。另有 7 个动作密集或构图存在争议的样本标为待产品负责人复核，只用于诊断。
- 新增双语 `/regression` 视觉回归实验室：可以完整运行本地 MediaPipe、叠加绿色初标保护框与紫色预测框，并导出 JSON。
- 保存未调参的 v0.2.7 原始预测、模型/配置哈希、失败案例和可重算指标；新增 `pnpm test:s2-regression` 离线质量门。v0.3.0 回归工具提交为 `e3ceabe1eb401b89e9ff4307d093824b9e2b35da`，运行参考配置来自 v0.2.7 提交 `bdf66d1db7511f97feba49713f9995ea6ef13711`。
- 完整方法、数字和限制记录在 `docs/S2_REGRESSION_BASELINE.md`；素材清单、README、案例说明、开发文档和路线图同步更新。

### 验证

- 20 张样本全部完成推理，模型不可用率 `0/20`。
- 13 个规则明确初标样本：安全投放 `6/13 = 46.2%`；危险位置误投 `4/13 = 30.8%`；过度顺延 `3/13 = 23.1%`。
- 保护目标匹配：精确率 `4/25 = 16.0%`；召回率 `4/11 = 36.4%`；F1 `22.2%`。这里既包含真人，也包含当前 COCO 类别表不擅长但产品必须保护的机器人和关键物体。
- 单次 Windows/Chromium 运行推理耗时：P50 `318 ms`，P95 `335 ms`；耗时只供诊断，不能当作跨设备 SLA。
- 危险位置误投样本：`charge-005`、`charge-008`、`charge-013`、`charge-018`。
- 过度顺延样本：`charge-002`、`charge-012`、`charge-016`。
- 完整 `pnpm check` 已通过：ESLint、TypeScript 类型检查、7 个 Vitest 文件中的 43 项测试、生产构建，以及 6 项渲染/依赖补丁测试全部通过。

### 遗留

- 上述数字只能称为“固定回归集一致率”，不能称为通用模型准确率。
- 当前结果刻意保留调参前的真实问题，不为了简历把低分隐藏或改成好看的数字；13 个规则明确标签仍只是代理初标，7 个 `needs-user-review` 诊断帧需要产品负责人复核后才能进入正式阻断指标。
- 位置评分器按 `0.3 × 0.24` 候选区域计算风险，而页面实际渲染的小卡片占位约为 `0.3 × 0.3`；这会让评分器低估真实遮挡面积，阶段 1B 必须统一计算几何与渲染 footprint。
- 当前 BlazeFace + EfficientDet 会把机器人或特效误识别成人脸/人物，也会漏掉罐体、能量效果等显著物体；重复框合并仍需校准。
- 普通 CI 只校验图片字节、标注合同、评分器和已保存原始预测能否重算；MediaPipe WASM 仍来自 jsDelivr，因此新鲜浏览器推理暂不作为强制联网 CI。

### 下一步

1. 由产品负责人复核 7 个争议样本，并抽查 13 个规则明确初标；复核前始终保留 `draft` / `needs-user-review` 身份。
2. 进入阶段 1B：先统一评分器 `0.3 × 0.24` 与页面约 `0.3 × 0.3` 的真实广告占位，再调整检测阈值、重复框合并和最小尺寸。
3. 以“不增加 `charge-005/008/013/018` 之外的危险误投，并优先消除现有危险误投”为第一门槛；每次修改都必须输出相对 v0.2.7 `bdf66d1` 参考配置的前后对比。
